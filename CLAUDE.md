# CLAUDE.md — dealligence

Авторитетная память проекта. Читать первым каждую сессию.

## Что это
Движок семантического анализа B2B-переписки. Корпус → атомы → таймлайн →
State Vector сделки → политики/нарративы/ассистент. Наследник Smart Sales
по принципу, не по коду. Первый кейс — WhiteBIT. Переиспользуется для sales ops.

## Назначение и цели
- ЦЕЛЬ 1 — систематизация: корпус разобран на атомы, разложен в таймлайн,
  declared/observed расхождения детектируемы.
- ЦЕЛЬ 2 — решения: State Vector сделки → рекомендуемые действия, нарративы,
  ассистент переговоров.
- Переиспользуемость: тот же движок для sales ops (Aluprof и далее).

## Принципы (наследуются от Smart Sales)
- LLM только интерпретирует, факты не выдумывает.
- Каждый атом привязан к дословной цитате (raw_text). Нет цитаты — нет атома.
- Граница детерминизма: атомы и таймлайн фиксированы, выше — LLM-reasoning.
- Audit trail: каждое значение оси состояния несёт причину из атома.
- N=1: WB — один контрагент. Это онтология/каркас для решений, НЕ обучаемая
  политика. POMDP/RL из sales_ai не переносится. Переносится язык описания.

## Machine map
- skufs-mac-mini — рабочая машина, мастер кода. `~/dealligence/`.
  SSH: `ssh -i ~/.ssh/id_ed25519_claude -o IdentitiesOnly=yes skufs-mac-mini`
  (обычный `ssh skufs-mac-mini` тоже, если agent жив).
- MacBook — зеркало/документы. Desktop Commander без SSH пишет на MacBook.
- LLM: orchestrator `http://localhost:4700` `/v1/llm/complete`, header
  `X-API-Key`, поле `agent="dealligence"` для биллинга. Ключ в `~/dealligence/.env`.
- Биллинг: `GET /v1/billing/summary?period=month`. Месячный лимит $5.

## Запреты
- Прямые вызовы api.anthropic.com / api.openai.com — ЗАПРЕЩЕНЫ. Только orchestrator.
- frv GitLab — READ-ONLY. Писать только в github `avrich5/dealligence`.
- Клиентские данные (корпус) в git НЕ коммитить. corpus_raw/corpus_txt/out в .gitignore.
- Не выдумывать факты. Атом без дословной цитаты отклоняется автоматически.
- Heredoc через SSH ломается. Писать локально + scp, либо python-файлами.
- Выбор LLM vs детерминированный метод — решает Andriy, не AI. Предлагать
  оба варианта с тремя осями: (1) стоимость прогона, (2) риск галлюцинаций
  для проекта, (3) ценность времени Andriy vs автоматизация. Самостоятельно
  выбирать детерминизм «ради экономии бюджета» — запрещено.

## Авторитетные документы (читать в этом порядке, не пропускать)

| Файл | Что содержит | Менять |
|---|---|---|
| CLAUDE.md | машинная память сессий, принципы, запреты | append-only |
| PROGRESS.md | статус тасков, одна строка на таск | по завершении таска |
| GROUNDTRUTH.md | верифицированные факты по корпусу | только corpus-grounded |
| DECISIONS.md | проектные решения, отложенные вопросы | каждое новое решение |
| concept/00_CONCEPT.md | что строим, архитектура | редко, с обоснованием |

Гейты живут внутри тасок (tasks/CLAUDE_CODE_TASK_NN_*.md).
CHECKLIST.md — ретроспектива пройденных гейтов, не источник истины.
Шаблон отчёта — docs/templates/SESSION_REPORT_TEMPLATE.md.

## Цикл работы (каждая сессия Claude Code)
1. Прочитать: CLAUDE.md → PROGRESS.md → GROUNDTRUTH.md → DECISIONS.md.
2. Взять один таск из tasks/. Не смешивать аналитику и реализацию.
3. Проверить баланс биллинга ДО прогона (если будут LLM-вызовы).
4. Сделать шаг. Каждый факт — со ссылкой на источник (файл:строка или атом id).
5. Завершить session-отчётом по docs/templates/SESSION_REPORT_TEMPLATE.md → reports/.
6. Обновить одну строку PROGRESS.md.
7. Новое решение → одна запись в DECISIONS.md.

## Состояние артефактов (append-only журнал)

### 2026-06-27 — TASK 02: сходимость онтологии
- 4 скрипта в scripts/: axes_distribution, cluster_axes, cluster_types,
  apply_decisions, orphan_patch, build_ontology_yaml, normalize_atoms.
- 478 сырых осей → 74 канонических (LLM-кластеризация + ручные решения Andriy).
- 85 сырых типов → 9 канонических.
- Orphan patch: 235 осей не попали в LLM-кластеризацию, сопоставлены вручную.
  Покрытие поднято с 88.2% до 99.2% (2107/2124 атомов).
- Артефакты: config/ontology.yaml, out/atoms_canon.jsonl (поля about_canon/type_canon).
- Детектор трёхпозиционный: resolved / observed_absent / unresolved.
  Зафиксировано в GROUNDTRUTH §6.
- Гипотеза доказана в живом примере: атрибуция «status update» → Дима (corpus),
  не Andriy. Человеческая память потеряла источник, система нашла дословно.
### 2026-06-26 — Каркас + атомизация корпуса
- ingest: LibreOffice (.docx/.pages) + pdfminer (text PDF), дедуп по stem.
  34/43 файла нормализованы. 2 фейла: пустой скан-PDF, .key (низкий приоритет).
- atomizer: эмерджентная онтология, anti-fabrication, LLM через orchestrator.
- Прогон всего корпуса: 2124 атома, 70 LLM-вызовов, $0.1155 (deepseek).
- Сырая онтология: 85 типов, 478 осей. Топ-оси (выведены из данных):
  scope 282, execution_control 192, data_sharing 144, revshare 102,
  timeline 91, exclusivity 42, trust 41, internal_authority 15.
- modality: declared 2047, done 68, observed_absent 3. Подтверждено:
  observed_absent НЕ выводится из одного файла — нужен межфайловый детектор
  по таймлайну. Это следующий слой, не атомизатор.

## Ключевые факты по WB-сделке (проверены по корпусу, не по памяти)
- SLA подписан ТОЛЬКО нашей стороной (печать MMI + подпись Chyrva).
  Подписи Носова в нашей копии нет, Effective Date пуст. Асимметрия обязательств.
- revshare 50% Net Trading Commission зафиксирован в тексте SLA §5.1 (не «устно одобрен»).
- Реквизитный дефект: reg.number Clear White на стр.1 (75407221) ≠ стр.9 (72229468).
- Дима: «не везде я могу последнее слово иметь» — ограничение его полномочий (синк).
- Дима: команда Влада работает, но не присылает status update («минус большой»).
- NDA подписан, билингв укр/англ. Класс сигнала declared/observed:
  устное согласие WB на передачу user_id + portfolio snapshot есть,
  в реальных запросах НЕ наблюдается (твоё устное свидетельство, в корпусе текстово
  не подтверждено — помечать confidence_source=oral_andriy).

## Gates
Гейты каждого таска — внутри tasks/CLAUDE_CODE_TASK_NN_*.md.
CHECKLIST.md — ретроспектива (только закрытые [x] с датой), не живой чеклист.
